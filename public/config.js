// Safe same-origin defaults for a checkout build. Deployment staging replaces
// this file with target-specific values from the authored protocol contract.
globalThis.AVALON_CONFIG = Object.freeze({ apiBase: '', apiProtocol: 3 });
