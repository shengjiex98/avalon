// Bump when a view or action changes so a deployed browser cannot handle it.
// Browser staging writes this value into generated runtime configuration so
// self-hosted and Pages clients share this one authored protocol number.
export const API_PROTOCOL = 3;
